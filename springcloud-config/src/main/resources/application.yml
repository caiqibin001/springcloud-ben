server:
  port: 4001
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/caiqibin001/123
          search-paths: {application}
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka